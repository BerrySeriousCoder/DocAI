// Public API — types only (initial scaffold)
export type {
    TextItemPosition,
    PagePositionData,
    PdfPositionIndex,
    ResolvedHighlight,
    ResolveOptions,
    CompressedPositionIndex,
    Logger,
} from './types.js';
